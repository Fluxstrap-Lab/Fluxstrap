<ui:UiPage x:Class="Fluxstrap.UI.Elements.Settings.Pages.CrashLogViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Fluxstrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:Fluxstrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:Fluxstrap.Resources"
      d:DataContext="{d:DesignInstance dmodels:CrashLogViewerViewModel, IsDesignTimeCreatable=True}"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="CrashLogViewerPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static resources:Strings.Menu_CrashLogs_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <ui:Button Margin="0,0,8,0" Icon="Folder24" Content="{x:Static resources:Strings.Menu_CrashLogs_OpenFolder}" Command="{Binding OpenCrashFolderCommand}" />
                <ui:Button Icon="ArrowSync24" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand}" />
            </StackPanel>
        </Grid>

        <ui:ProgressRing Margin="0,40,0,0" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <StackPanel Visibility="{Binding ShowList, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="0,16,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Card Grid.Column="0" Margin="0,0,8,0" Padding="8,4">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{x:Static resources:Strings.Menu_CrashLogs_TotalLogs}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding LogFiles.Count}" FontSize="14" FontWeight="Bold" Margin="6,0,16,0" VerticalAlignment="Center" />
                        <TextBlock Text="{x:Static resources:Strings.Menu_CrashLogs_TotalSize}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding TotalSize}" FontSize="14" FontWeight="Bold" Margin="6,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:Card>
                <ui:Button Grid.Column="1" Margin="0,0,8,0" Icon="Delete24" Content="{x:Static resources:Strings.Menu_CrashLogs_DeleteAll}" Command="{Binding DeleteAllCommand}" Visibility="{Binding HasLogs, Converter={StaticResource BooleanToVisibilityConverter}}" />
            </Grid>

            <ListView ItemsSource="{Binding LogFiles}" SelectedItem="{Binding SelectedLog}" MaxHeight="250" Visibility="{Binding HasLogs, Converter={StaticResource BooleanToVisibilityConverter}}">
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="{x:Static resources:Strings.Common_Date}" Width="140">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding DateFormatted}" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{x:Static resources:Strings.Common_File}" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FileName}" TextWrapping="Wrap" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="{x:Static resources:Strings.Common_Size}" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SizeFormatted}" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>

            <ui:Card Margin="0,8,0,0" Padding="12" Visibility="{Binding ShowLogViewer, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding SelectedLog.FileName}" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center" />
                        <ui:Button Grid.Column="1" Icon="Dismiss24" Command="{Binding CloseLogViewerCommand}" />
                    </Grid>
                    <Border Margin="0,8,0,0" Background="{DynamicResource ControlFillColorDefaultBrush}" Padding="8" MaxHeight="300">
                        <ScrollViewer>
                            <TextBlock Text="{Binding LogContent}" FontFamily="Consolas" FontSize="12" TextWrapping="Wrap" />
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </ui:Card>

            <TextBlock Margin="0,16,0,0" Text="{x:Static resources:Strings.Menu_CrashLogs_Empty}" FontSize="14" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Visibility="{Binding HasLogs, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
        </StackPanel>
    </StackPanel>
</ui:UiPage>
