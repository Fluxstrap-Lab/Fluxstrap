<ui:UiPage x:Class="Fluxstrap.UI.Elements.Settings.Pages.StatisticsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Fluxstrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:Fluxstrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:Fluxstrap.Resources"
      d:DataContext="{d:DesignInstance dmodels:StatisticsViewModel, IsDesignTimeCreatable=True}"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="StatisticsPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static resources:Strings.Menu_Statistics_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
            <ui:Button Grid.Column="1" Icon="ArrowSync24" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand}" />
        </Grid>

        <Grid Margin="0,16,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ui:Card Grid.Row="0" Grid.Column="0" Margin="0,0,4,8">
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Today}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="{Binding TotalPlayTimeToday}" FontSize="24" FontWeight="Bold" Margin="0,4,0,0" />
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Row="0" Grid.Column="1" Margin="4,0,4,8">
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_AllTime}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="{Binding TotalPlayTimeAllTime}" FontSize="24" FontWeight="Bold" Margin="0,4,0,0" />
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Row="0" Grid.Column="2" Margin="4,0,4,8">
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_Sessions}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="{Binding TotalSessions}" FontSize="24" FontWeight="Bold" Margin="0,4,0,0" />
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Row="0" Grid.Column="3" Margin="4,0,0,8">
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_AverageSession}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="{Binding AverageSessionTime}" FontSize="24" FontWeight="Bold" Margin="0,4,0,0" />
                </StackPanel>
            </ui:Card>

            <ui:Card Grid.Row="1" Grid.ColumnSpan="4" Margin="0,0,0,0">
                <StackPanel>
                    <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_MostPlayed}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                    <TextBlock Text="{Binding MostPlayedGame}" FontSize="20" FontWeight="SemiBold" Margin="0,4,0,0" />
                </StackPanel>
            </ui:Card>
        </Grid>

        <TextBlock Text="{x:Static resources:Strings.Menu_Statistics_TopGames}" FontSize="20" FontWeight="Medium" Margin="0,16,0,8" />

        <ListView ItemsSource="{Binding TopGames}" MaxHeight="400">
            <ListView.Resources>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.Resources>
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="{x:Static resources:Strings.Common_Game}" Width="250">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding GameName}" TextWrapping="Wrap" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static resources:Strings.Menu_Statistics_Sessions}" Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding SessionCount}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static resources:Strings.Menu_Statistics_PlayTime}" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TotalPlayTimeFormatted}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="{x:Static resources:Strings.Menu_Statistics_LastPlayed}" Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding LastPlayed}" HorizontalAlignment="Center" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        
        <TextBlock Margin="0,8,0,0" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{x:Static resources:Strings.Menu_Statistics_Note}" TextWrapping="Wrap" />
    </StackPanel>
</ui:UiPage>
