<ui:UiPage x:Class="Fluxstrap.UI.Elements.Settings.Pages.VersionManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:Fluxstrap.UI.Elements.Controls"
      xmlns:dmodels="clr-namespace:Fluxstrap.UI.ViewModels.Settings"
      xmlns:resources="clr-namespace:Fluxstrap.Resources"
      d:DataContext="{d:DesignInstance dmodels:VersionManagerViewModel, IsDesignTimeCreatable=True}"
      mc:Ignorable="d"
      d:DesignHeight="600" d:DesignWidth="800"
      Title="VersionManagerPage"
      Scrollable="True">

    <StackPanel Margin="0,0,14,14">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{x:Static resources:Strings.Menu_VersionManager_Description}" FontSize="14" Foreground="{DynamicResource TextFillColorSecondaryBrush}" VerticalAlignment="Center" />
            <ui:Button Grid.Column="1" Icon="ArrowSync24" Content="{x:Static resources:Strings.Common_Refresh}" Command="{Binding RefreshCommand}" />
        </Grid>

        <ui:ProgressRing Margin="0,40,0,0" IsIndeterminate="True" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <StackPanel Visibility="{Binding ShowContent, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid Margin="0,16,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Card Grid.Column="0" Margin="0,0,4,0" Padding="12">
                    <StackPanel>
                        <TextBlock Text="{x:Static resources:Strings.Menu_VersionManager_TotalSize}" FontSize="12" Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                        <TextBlock Text="{Binding TotalSize}" FontSize="20" FontWeight="Bold" Margin="0,4,0,0" />
                    </StackPanel>
                </ui:Card>
                <ui:CardAction Grid.Column="1" Margin="4,0,0,0" Icon="Folder24" Command="{Binding OpenVersionsFolderCommand}">
                    <StackPanel>
                        <TextBlock FontSize="14" Text="{x:Static resources:Strings.Menu_VersionManager_OpenFolder}" TextWrapping="Wrap" />
                        <TextBlock Margin="0,2,0,0" FontSize="12" Text="{x:Static resources:Strings.Menu_VersionManager_OpenFolder_Description}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" TextWrapping="Wrap" />
                    </StackPanel>
                </ui:CardAction>
            </Grid>

            <ui:Button Margin="0,0,0,8" Icon="Delete24" Content="{x:Static resources:Strings.Menu_VersionManager_DeleteOld}" Command="{Binding DeleteOldVersionsCommand}" Visibility="{Binding HasVersions, Converter={StaticResource BooleanToVisibilityConverter}}" />

            <ListView ItemsSource="{Binding Versions}" MaxHeight="500">
                <ListView.Resources>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.Resources>
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Version" Width="200">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding VersionGuid}" TextWrapping="Wrap" />
                                        <ui:Badge Margin="6,0,0,0" Content="Active" Visibility="{Binding IsActive, Converter={StaticResource BooleanToVisibilityConverter}}" Appearance="Success" />
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Type" Width="110">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding BinaryType}" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Installed" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding InstalledDate}" HorizontalAlignment="Center" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Size" Width="90">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding SizeFormatted}" HorizontalAlignment="Center" FontWeight="SemiBold" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </StackPanel>
    </StackPanel>
</ui:UiPage>
